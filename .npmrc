# Always save exact versions in package.json
save-exact=true

# Optional: Increase fetch timeout if you encounter network issues
fetch-timeout=60000

# Optional: Set strict SSL, disable if you encounter SSL errors
strict-ssl=true

# Ensure global dependencies are installed correctly (helpful for some environments)
global-style=true
